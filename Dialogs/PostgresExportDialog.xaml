<Window x:Class="LayoutEditor.Dialogs.PostgresExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export to PostgreSQL" Height="400" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Layout Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Layout Name:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="LayoutNameBox" Text="Untitled Layout"/>
        </StackPanel>

        <!-- Option 1: SQL File -->
        <GroupBox Grid.Row="1" Header="Option 1: Save as SQL File" Margin="0,0,0,10">
            <StackPanel Margin="10">
                <TextBlock Text="Generate a .sql script file that can be imported later." 
                           TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,10"/>
                <Button x:Name="SaveSqlButton" Content="Save SQL File..." 
                        Click="SaveSqlFile_Click" Padding="15,8" HorizontalAlignment="Left"/>
            </StackPanel>
        </GroupBox>

        <!-- Option 2: Direct Database -->
        <GroupBox Grid.Row="2" Header="Option 2: Export to Database" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Host:" VerticalAlignment="Center" Grid.Row="0"/>
                <TextBox x:Name="HostBox" Text="localhost" Grid.Row="0" Grid.Column="1" Margin="0,2"/>
                <TextBlock Text="Port:" VerticalAlignment="Center" Grid.Row="0" Grid.Column="2" Margin="10,0,5,0"/>
                <TextBox x:Name="PortBox" Text="5432" Grid.Row="0" Grid.Column="3" Margin="0,2"/>

                <TextBlock Text="Database:" VerticalAlignment="Center" Grid.Row="1"/>
                <TextBox x:Name="DatabaseBox" Text="layout_db" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Margin="0,2"/>

                <TextBlock Text="Username:" VerticalAlignment="Center" Grid.Row="2"/>
                <TextBox x:Name="UsernameBox" Text="postgres" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Margin="0,2"/>

                <TextBlock Text="Password:" VerticalAlignment="Center" Grid.Row="3"/>
                <PasswordBox x:Name="PasswordBox" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,2"/>
                <Button x:Name="ExportDbButton" Content="Export" Grid.Row="3" Grid.Column="3" 
                        Click="ExportToDatabase_Click" Margin="5,2,0,2"/>
            </Grid>
        </GroupBox>

        <!-- Status -->
        <TextBlock x:Name="StatusText" Grid.Row="3" Foreground="Gray" 
                   TextWrapping="Wrap" VerticalAlignment="Top"/>

        <!-- Close Button -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save Schema..." Click="SaveSchema_Click" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Close" IsCancel="True" Padding="15,8"/>
        </StackPanel>
    </Grid>
</Window>
